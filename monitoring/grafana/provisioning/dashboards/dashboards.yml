apiVersion: 1

providers:
  - name: PDF RAG
    orgId: 1
    folder: PDF RAG
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
